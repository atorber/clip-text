name: ClipText
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  sqflite: ^2.3.0
  path: ^1.8.3
  path_provider: ^2.1.2
  uuid: ^4.2.1
  system_audio_recorder:
    path: system_audio_recorder
  just_audio: ^0.9.36
  receive_intent: ^0.2.5
  shared_preferences: ^2.2.2
  http: ^1.2.1
  crypto: ^3.0.3
  convert: ^3.1.1
  markdown_widget: ^2.3.2+8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true